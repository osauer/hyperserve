# HyperServe Configuration Example
# Copy this file to .env and modify as needed

# Server Configuration
HS_PORT=8080
HS_ADDR=:8080

# Rate Limiting
HS_RATE_LIMIT=100
HS_BURST_LIMIT=200

# Timeouts (Go duration format: 1s, 1m, 1h)
HS_READ_TIMEOUT=30s
HS_WRITE_TIMEOUT=30s
HS_IDLE_TIMEOUT=120s

# TLS Configuration (optional)
# HS_TLS_CERT_FILE=/path/to/cert.pem
# HS_TLS_KEY_FILE=/path/to/key.pem

# Directories (optional)
# HS_STATIC_DIR=./static
# HS_TEMPLATE_DIR=./templates

# JSON Config File (optional)
# HS_CONFIG_PATH=./config.json

# Feature Flags
# HS_CHAOS_MODE=false
# HS_HEALTH_SERVER=true